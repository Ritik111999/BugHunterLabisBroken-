# Core voiceprint stack (intro enrollment + live matching).
# Install: scripts/setup-voiceprint.sh
numpy
torch
torchaudio
speechbrain

# Optional noise suppression (needs Rust/cargo to build deepfilterlib on some Macs).
# deepfilternet
